# Krakens Canvas Icon

Save the app icon image as `icon.png` in this folder.

Krakens Canvas is already wired to use:

```html
assets/icon.png
```

After replacing the icon file, close and reopen the local app window so Edge refreshes the app icon.
